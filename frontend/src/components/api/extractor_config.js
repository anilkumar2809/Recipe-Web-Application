// configuration for backend server running flask
const extractor_config = {
  serverAddress: "http://127.0.0.1:5000/",
  port: null,
  basePath: "http://127.0.0.1:5000/",
};

export default extractor_config;